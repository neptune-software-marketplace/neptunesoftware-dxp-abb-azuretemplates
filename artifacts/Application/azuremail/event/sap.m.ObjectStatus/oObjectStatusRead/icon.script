if (typeof isRead === "undefined" || isRead === null || isRead === "") { return; }  
var formattedText = isRead; 

if (formattedText) return "sap-icon://email-read";
else return "sap-icon://email";